services:
  - type: web
    name: agro-connect-backend
    runtime: go
    rootDir: backend
    buildCommand: go build -o server .
    startCommand: ./server
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
